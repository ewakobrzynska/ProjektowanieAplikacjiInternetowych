<style>
  body {
    font-family: Arial, sans-serif;
    background-color: #f5f5f5;
  }

  .container {
    max-width: 960px;
    margin: 40px auto;
    padding: 20px;
    background-color: #ffffff;
    border: 1px solid #dddddd;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  }

  h1 {
    margin-top: 0;
    font-weight: bold;
    color: #337ab7;
  }

  table {
    width: 100%;
    border-collapse: collapse;
  }

  th,
  td {
    padding: 10px;
    border: 1px solid #dddddd;
  }

  th {
    background-color: #f5f5f5;
    font-weight: bold;
  }

  tr:nth-child(even) {
    background-color: #f5f5f5;
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  li {
    margin-bottom: 5px;
  }
</style>

<div class="container">
  <h1>LISTA ZAKOŃCZONYCH PRZETARGÓW</h1>

  <table>
    <thead>
      <tr>
        <th>Tytuł</th>
        <th>Opis</th>
        <th>Instytucja</th>
        <th>Start</th>
        <th>Koniec</th>
      </tr>
    </thead>
    <tbody>
      <% closedTenders.forEach(tender => { %>
        <tr>
          <td><a href="/closed-tenders/<%= tender.id %>">S<%= tender.title %></td>
          <td><%= tender.description %></td>
          <td><%= tender.institution %></td>
          <td><%= moment(tender.start_time).format('YYYY-MM-DD HH:mm') %></td>
          <td><%= moment(tender.end_time).format('YYYY-MM-DD HH:mm') %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>